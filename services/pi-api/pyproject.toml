[project]
name = "secure-home-pi-api"
version = "0.0.0"
description = "Household read and command surface; the governed enforcement point."
readme = "README.md"
requires-python = ">=3.13"
# No runtime dependencies. See AGENTS.md — adding one is a reviewed decision.
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/secure_home_pi_api"]
