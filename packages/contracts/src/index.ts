/**
 * Authored source for API and domain-facing contracts (ADR-0012 §7).
 *
 * Empty: the Zod contracts are issue #28. This file exists so the package
 * boundary compiles and can be depended on; it declares no behaviour.
 */

export {}
