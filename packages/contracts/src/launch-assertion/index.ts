/** Contract family: the launch assertion (capability `runner-execution`). */
export {
  LAUNCH_ASSERTION_ID,
  LAUNCH_ASSERTION_VERSION,
  LaunchAssertion,
} from './launch-assertion.js'
export type { LaunchAssertionT } from './launch-assertion.js'
