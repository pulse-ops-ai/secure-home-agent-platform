/**
 * Shared test helpers and fixtures.
 *
 * Empty: helpers arrive with the first suite that needs more than one
 * consumer. There is deliberately no test framework chosen here yet.
 */

export {}
