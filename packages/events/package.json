{
  "name": "@secure-home/events",
  "version": "0.0.0",
  "private": true,
  "description": "Run event and evidence contracts, and stable event names.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@secure-home/contracts": "workspace:*",
    "zod": "catalog:"
  },
  "scripts": {
    "lint": "pnpm run deps && eslint src",
    "typecheck": "pnpm run deps && tsc --noEmit",
    "test": "pnpm run deps && vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json",
    "generate": "pnpm run deps && tsc -p tsconfig.build.json && node dist/tools/generate.js",
    "deps": "pnpm --filter @secure-home/contracts run build"
  },
  "devDependencies": {
    "@secure-home/eslint-config": "workspace:*",
    "@secure-home/testing": "workspace:*",
    "@secure-home/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
