/**
 * Structured logging and request context (ADR-0012 §14).
 *
 * Empty: Winston and AsyncLocalStorage wiring arrive with the first service
 * that needs them. No logger is configured here.
 */

export {}
