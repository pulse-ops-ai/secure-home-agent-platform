{
  "name": "@secure-home/adapter-claude-code",
  "version": "0.0.0",
  "private": true,
  "description": "Claude Code reference adapter: pure translation between the frozen adapter SPI and the pinned @anthropic-ai/claude-code CLI. Translates and reports; never decides.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./bin": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {},
  "scripts": {
    "lint": "pnpm run deps && secure-home-lint src",
    "typecheck": "pnpm run deps && tsc --noEmit",
    "test": "pnpm run deps && vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json",
    "deps": "pnpm --filter @secure-home/contracts run build"
  },
  "devDependencies": {
    "@secure-home/contracts": "workspace:*",
    "@secure-home/eslint-config": "workspace:*",
    "@secure-home/lint-config": "workspace:*",
    "@secure-home/testing": "workspace:*",
    "@secure-home/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
