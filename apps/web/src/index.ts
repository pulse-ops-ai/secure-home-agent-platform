/**
 * Household web application (ADR-0012 §4).
 *
 * BOUNDARY ONLY. No Next.js scaffold, no route, no page, no BFF — that needs
 * its own issue. This file exists so the boundary is discoverable.
 */

export {}
