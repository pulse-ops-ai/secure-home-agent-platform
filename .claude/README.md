# .claude/

Generated Claude Code integration for the OpenSpec workflow — six skills
(`skills/openspec-*`) and six slash commands (`commands/opsx/*`:
`propose`, `apply`, `archive`, `explore`, `sync`, `update`).

Generated by `openspec init --tools claude` (OpenSpec 1.8.0). Regenerate with
`openspec update` after an OpenSpec upgrade; do not hand-edit the generated
files — edits are lost on regeneration.

Like [`CLAUDE.md`](../CLAUDE.md), this directory is a provider-specific
**adapter**: it routes an agent into the OpenSpec planning workflow and grants
nothing. The contract remains [`AGENTS.md`](../AGENTS.md) and the accepted
ADRs, and OpenSpec artifacts stay planning-only (see
[`openspec/README.md`](../openspec/README.md)).
